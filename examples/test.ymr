target Swagger, JavaScript_ExpressJS;

use json;

use env;

use cors(
	origin: @env.ALLOWED_ORIGIN
);

auth Bearer as apiKey (
    source: header,
    defaultAccess: authenticated,
    mode: "FULL"
);

router /auth 
    as apiRouter
{
    // Prints the Hello message with the Name concatenated
    GET /hello/:name
        as HelloRoute

    POST /person
        as CreatePerson
        body (
            name: string,
            age: int,
            address: {
                zipCode: int,
                street: string
            }
        )

    router /v2
    {
        include "include.ymr"
    }
}

router /another-router
{
    GET /test;
}