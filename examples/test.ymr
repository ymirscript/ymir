target JavaScript;

use env;

use cors(
	origin: @env.ALLOWED_ORIGIN
);

router /api 
    as apiRouter
{
    GET /hello?name=string&age=int
        as HelloRoute

    include "include.ymr"
}